### 一、图片优化
#### 1、图片查询优化
+ 缓存——读多写少场景（高频访问、计算成本高、短时延时）
  + Redis分布式缓存
  + Caffeine本都缓存
+ 模板模式多个缓存模型实现
```java
// 定义抽象类，统一缓存管理
public abstract class CacheManager<V> {
    // 定义基本四种缓存操作（读、写、删、查），供子类实现
    protected abstract V doGet(String key);
    protected abstract void doPut(String key, V value, long expireSeconds);
    protected abstract void doDelete(String key);
    protected abstract boolean doExists(String key);
    
    // 父类基础属性和方法
    // 拼接缓存Key前缀，项目隔离，可以为空
    private final String name;
    // 缓存时间相关属性，单位秒
    // 子类实例默认缓存时间，创建实例时指定
    private final long defaultExpireSeconds;
    // 类默认缓存时间，实例未指定时
    private static final long DEFAULT_EXPIRE_SECONDS = 30 * 60L;
    // 缓存时间随机偏移因子，上下偏移20%，防止缓存雪崩
    private static final double EXPIRE_RANDOM_OFFSET_FACTOR = 0.2;
    private static final long MIN_EXPIRE_SECONDS = 1;
    
    // 子类实例调用方法
    // 1、get方法——需要完成缓存未命中时的数据库查询
    public V get(String key, Function<String, V> loader, long expireSeconds) {
        // 1、与前缀拼接获取完整key
        String cacheKey = name + key;
        // 2-1、缓存命中，返回缓存数据
        V value = doGet(cacheKey);
        // 2-2、缓存未命中，调用loader方法查询数据库，设置过期时间
        // TODO: 可在此处解决缓存穿透问题
    }
    // 2、put方法
    public void put(String key, V value, long expireSeconds) {
        // 1、与前缀拼接获取完整key
        String cacheKey = name + key;
        // 2、设置缓存数据和过期时间
        doPut(cacheKey, value, expireSeconds);
    }
    // 3、delete方法
    public void delete(String key) {
        // 1、与前缀拼接获取完整key
        String cacheKey = name + key;
        // 2、删除缓存数据
        doDelete(cacheKey);
    }
    // 4、exists方法
    public boolean exists(String key) {
        // 1、与前缀拼接获取完整key
        String cacheKey = name + key;
        // 2、判断缓存数据是否存在
        return doExists(cacheKey);
    }
}
```